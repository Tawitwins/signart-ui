import { MyformatcurrencyPipe } from './myformatcurrency.pipe';

describe('MyformatcurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new MyformatcurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
