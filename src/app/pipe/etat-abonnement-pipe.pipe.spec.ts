import { EtatAbonnementPipePipe } from './etat-abonnement-pipe.pipe';

describe('EtatAbonnementPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EtatAbonnementPipePipe();
    expect(pipe).toBeTruthy();
  });
});
